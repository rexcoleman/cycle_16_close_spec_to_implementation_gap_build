# Cycle-16-S26 reality-vs-intent diagnostic (HC #70) — faithful-target reconstruction (Done #60, past-era)

Per `feedback_cycle_close_reality_vs_intent_diagnostic.md`: name what the mechanism ACTUALLY does vs what the directive asked; tag numbers measured/heuristic/anecdotal; refuse comfortable framings.

| Piece | Claim | tag | Evidence ref (Coach re-ran) | Honest gap |
|---|---|---|---|---|
| Faithful-target extraction | Each spec's implementation target extracted from its OWN text by 2 independent readers (E1 rule-based + E2 LLM:claude-haiku-4-5); single-reader REFUSED | [measured] | `outputs/faithful_target_map.json` (n=206; `extraction_methods_distinct=true`); Coach re-ran extractor → dispositions {agreed_faithful_target:2, dp26_carveout_preserved:203, reader_disagreement_surfaced_conservative:1, defect:0} | Reader accuracy = E1∩E2 agreement + independent verifier reading spec text; NOT validated vs blind-labeled ground truth. The 203 DP#26 carve-out classification is INHERITED from the BE-D scan, NOT re-validated this session |
| Population at risk | Exactly 3 specs had a REAL (non-n/a) synthesized target — the only specs a synthesized-class defect could affect | [measured] | Coach filtered `faithful_target_map.json`: 3 real (`af3a918a, 62652036, e14a3ffc`); matches S24's 3 exec-tier (1 impl/2 not) | 203 carve-outs rest on the prior DP#26 classification; if that classification is wrong for any carve-out, a target is missed |
| No substitution | Zero specs with a real synthesized target were downgraded to n/a to dodge extraction | [measured] | Coach check: real→n/a downgrades = **0**; spec-provenance defects = **0** | "0 defects" means none of the 3 real-target specs is untraceable; the carve-outs are not audited for untraceability this session |
| The af3a918a correction | Synthesized `kernel_coach.dispatch` (absent from spec) → faithful `kernel_coach.transition.fire` (spec-of-record `.claude/agents/kernel-coach.md`) | [measured] | F-probe both directions: synthesized→`not_faithful`, faithful→`faithful`; the synthesized string would false-flag a CORRECT impl | This is the exact S25 sub-finding, now corrected as a measurement; it does not retro-prove other specs |
| KG provenance capture | 3 faithful targets written to Fuseki /cycle6 with `prov:wasDerivedFrom` edges to named source artifacts (past-era, disclosed-confidence) | [measured] | `outputs/faithful_target_kg_readback.json`; Coach re-ran capture → read-back af3a918a `faithfulCommittedObservable=kernel_coach.transition.fire`, prov_edge=True, recon=`past_era_disclosed_confidence` | Reconstruction overlay = past-era fallback; the FORWARD authoring-time enforcement (#67 govML→Builder-ARCH) is NOT built (S27+) |
| Re-derived gap list | Gap list re-run vs faithful targets; probe UNMODIFIED (input fixed, not probe) | [measured] | `outputs/trusted_detector_run_faithful.json`; `git diff --stat` on probes/harness/floors/fixtures/`trusted_detector_run.json` = **EMPTY** | Re-derived to a NEW file; original synthesized gap list preserved |
| Independent verification | Re-derivation corroborated by an instrument independent of the extractor (different path; no import of extractor/F-probe) | [measured] | `scripts/faithful_rederivation_independent_verifier.py` → exit 0; Coach grep-confirmed no real import of extractor/F-probe/harness | Independent verifier reads spec text directly — shares the "spec text is ground truth" assumption with E1; both could be wrong if the spec text itself is wrong (out of scope this session) |
| Before/after movement | Exactly 1 verdict flip: af3a918a exec False→True (the synthesized-class false-negative); 62652036 target corrected but stays dormant (False); judgment tier 203/123 invariant | [measured] | re-derivation: exec impl 2 / not 1; agg **11/206=5.34%** (was 10/206=4.85%); +1 = af3a918a only | Not engineered in either direction; the +1 is a corrected false-negative, not new implementation |

## HC #70 honest scope statement — what S26 does NOT prove

- **Does NOT close the spec→implementation gap.** 2/206 execution-proven (was 1); ~195 judgment-tier remain CONTESTED/not-implemented. This is the measurement-fidelity fix.
- **Does NOT build the forward authoring-time KG enforcement** (#67 — provenance + finding-disposition captured at authoring, gate-enforced). kc-54 sequenced that to S27+ (RP-design → Builder-ARCH govML).
- **Does NOT re-validate the judgment tier** (203 specs; reused cached verdicts — legitimate, since the faithful overlay changes only the 3 exec-tier targets, leaving judgment inputs byte-identical; the 95.6% fail-safe CONTESTED status is unchanged and still an honest not-yet).
- **Does NOT re-validate the 203 DP#26 carve-out classification** — inherited from the BE-D scan; a disclosed residual.
- **Carries the residuals unchanged:** M1′=193 disclosed-bound; +53 DD classification-surplus; 6 single-reader edge specs; 26-absent extraction-coverage gap; BE-J 33.88% NOT promoted (Done #42 lock).
